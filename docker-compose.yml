services:
  youtube-downloader:
    build: .
    container_name: youtube-downloader
    ports:
      - "8000:8000"
    volumes:
      - ./downloads:/app/downloads
      - ./config:/app/config
    restart: unless-stopped
